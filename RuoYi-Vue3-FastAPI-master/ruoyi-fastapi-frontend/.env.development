# 页面标题
VITE_APP_TITLE = 工程系统

# 本地测试环境配置（Docker 容器中使用）
VITE_APP_ENV = 'local'

# 工程系统/本地测试环境
# 注意：前端代码在浏览器中执行，浏览器在宿主机上
# 后端容器映射了 9099 端口到宿主机，所以使用 localhost:9099
VITE_APP_BASE_API = '/dev-api'

# 是否在打包时开启压缩，支持 gzip 和 brotli
VITE_BUILD_COMPRESS = gzip
